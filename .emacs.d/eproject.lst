;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
	  '(("eproject" "/home/morose/.emacs.d/emacs-for-python/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
	  '((top . 27)
		(left . 0)
		(width . 114)
		(height . 31)))

